# Auto-Grader
Upload exercise details, model solution, rubric, and student submissions. Uses Groq embeddings (set GROQ_API_URL and GROQ_API_KEY). Applies JSON rubric or similarity-score mapping + grammar penalty. Outputs: score, reasoning, feedback, and error highlights.
